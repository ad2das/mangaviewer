# Astra Pro architecture review — 2026-09-05

## Authority and status

- User requires a fresh GPT-6 Astra Pro review. Do not use the prior model review as architectural authority.
- Subsequent user policy: use Oracle only for changes large enough to require structural redesign; diagnose, fix, and verify ordinary bugs directly. Wait for this already-submitted review before proceeding with its conclusions.
- Oracle skill origin: `https://github.com/ad2das/oracle-patched-package.git`.
- Fetched origin and fast-forward synchronization returned already up to date at `3e2ef94bcdfa3b2367b8de732ef8937842035f01`.
- Active review session: `mangaviewe-astra-owner-redesign`.
- Conversation: `https://chatgpt.com/c/6a9b7369-24a8-83ee-bb97-032dcf653972`.
- Completed after 23m23s. Wrapper verified requested GPT-6 Astra, resolved `6Pro`, strategy select, verified=yes. Session-scoped live reads and final transcript match the requested code review.
- Full answer read from `C:\Users\Administrator\.oracle\sessions\mangaviewe-astra-owner-redesign\artifacts\transcript.md`. Use this answer, not the prior model review. Advice still requires verification against actual code/tests.
- 124 current code files bundled; reported input estimate 135,718 tokens. Full original release scope included in prompt. No release success claimed; 200-episode count remains zero.

## Latest measured failure

Data-cold NTK: first actual content 7,591ms (4,000ms limit, FAIL). Manifest 5,923.8572ms, response 1,285.3748ms, transfer 60.9378ms, decode 69.4974ms, presentation 9.3829ms.

Parser stages: normalization 372ms, DOM 428ms, image scan 54ms, JSON 695ms. The earlier normalization rewrite has not demonstrated a parser performance win. Do not attribute differences between individual cold runs to it without controlled evidence.

Evidence: `.tmp-evidence-ntk-cold-parser-stages-20260905-4.txt` and matching `-logcat.txt`. Unified command 26497 completed with a test failure, despite shell exit zero.

## Independent harness audit while waiting (not model advice)

1. `ViewerTenEpisodeAutoAppendHarness.run` stops as soon as `observedEpisodes.size == requiredEpisodes`. That records entry into the last episode, not traversal of all its pages. Five episode entries do not prove five full episode traversals.
2. The corpus records a discovered expected chain but passes only the starting episode to its harness. Harness checks self-reported manifest adjacency, not equality with the independently discovered complete chain.
3. `withProductionDetailWarmup` explicitly waits 2,500ms and invokes the warmer directly. This is not actual UI navigation and cannot serve as proof of a data-cold or immediate-tap entry. Production prewarming and genuine UI dwell may be valid behaviors, but this test setup needs separate classification and cannot substitute for the strict cold gate.
4. `ViewerPresentationTraceVerifier` checks full visual coverage, which may include loading visuals, and merely requires some readable actual content in a gesture window. This alone does not establish every visible page is a correct image on every presented frame.
5. Corpus continues collecting diagnostic failures and fails at the end. Do not count later successful samples toward a consecutive release run after an earlier failure.

These are verification gaps, not evidence of newly observed device bugs. Cross-check the final Astra review and current code before implementing coherent changes. Preserve original thresholds and UI; no changes based on the older model review.

## Final Astra review implementation ledger

Keep the UI, ViewerSession, ViewerContentPipeline, one EGL/GLES renderer, source-neutral IDs, incremental ledger, and immutable ready-document FD replay. Do not introduce another scheduling owner or rebuild UI.

- [ ] P0-1: Collision-free installed scene identity; integer half-open source-row tile contracts; adjacent 6,001-row bands cannot remove each other.
- [ ] P0-2: Preserve intra-page pixels, not estimated-height fractions; deep resume before dimensions; drain input before HOME save; ordered reversal; unresolved-boundary runway.
- [ ] P0-3: Lossless latest-demand delivery; real promotion detection; CancelRequested until physical termination; completion/CPU/GL lease ownership and joined shutdown.
- [ ] P0-4: Sole bounded episode-demand owner in pipeline; remove implicit source next policies and independent runtime retry loop; current viewport precedence.
- [ ] P0-5: One NTK access flight; normal provider conditions/minSeen/identity; document epoch; successful cookie barrier; ready-only static cache; validated winner-only origin update.
- [ ] P0-6: Manifest-specific document reader with exact escape layers, conflict/completeness/order checks, no full-document double normalization, unchanged raw replay.
- [ ] P0-7: Verify actual presentation timestamp provenance and all terminal results against CURRENT native code (review claims must not be assumed); context loss; CPU/GPU/source lease lifetimes.
- [ ] Strict harness: full last episode, independent expected chain, cold versus real UI prewarm, every displayed image region, all owned-process memory, no preparation waits or skipped failures.
- [ ] Same-snapshot full test/lint/release/structure + fixed random 200-episode real-gesture evidence, then main commit/push.

Architecture alone cannot promise bounded external responses or arbitrary-speed boundary readiness. Do not relax the release gate or turn attribution into an exemption. Parser-only optimization cannot remove enough of the measured 7,591ms by itself.

## Implementation progress after final answer

- Reproduced scene-key collision in real app JVM test: two different windows/draw lists both yielded 954305. Replaced hash correctness check with GL-owned monotonic IDs and exact installed draw-list equality. Installation is acknowledged only after successful native submission; surface reattachment invalidates it. Six regression tests pass, including equal array hashes, failed submit, empty replacement, scroll-only reuse, and reattachment.
- Reproduced 6,001-row partition overlap in real content-module test: preceding bottom=2001 versus next top=2000. Introduced exact half-open `SourceRowRange` from decode planning through worker commands, decode state, native decode and test adapters. Fractional viewport demand is converted once to integer rows before selecting bands. Three policy tests pass including 500 deterministic randomized partial demands, plus existing pipeline suite.
- Passed `:viewer-content:test :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest verifyArchitectureQuality` after these changes. Kotlin daemon initially restarted during compilation but final command exited 0.
- Installed both debug APKs on emulator-5554. `NativeCpuDecodeFormatsInstrumentedTest` passed JPEG/PNG/WebP source-range and output-dimension checks. This is NOT a pixel-color comparison or a live-episode success. Evidence `.tmp-evidence-astra-integer-source-rows-20260905.txt`.
- Read CURRENT `gl_viewer_renderer.cpp`: display/latch/render-complete fallback and swap-return callback remain. The Astra warning applies to current code; do not rely on older conversational claims that fallback was removed. Provenance and terminal-result work remains pending.
- P0-1 still needs scene/session/native integration checks and visible pixel validation; do not mark full stage or release complete based on unit tests alone.
- Found `.gitignore` capture pattern `viewer-*` excluded the ENTIRE real `viewer-content` module from version control. Added a root module exception; build outputs remain ignored. This must be included in final snapshot/commit audit so a clean checkout can build the same pipeline.
- P0-2 pixel anchor: real JVM regressions failed before modification (height-only resolution moved scroll from250px to2500px; unchanged-layout round trip lost fixed-point precision). `SemanticViewportAnchor` now carries exact intra-page FixedPx plus basis viewport width. Restore scales only by viewport width, never estimated page height. Three regression tests pass. Existing property tests now assert exact same PageId and screen pixel, not approximate preservation of a height fraction; 1..500-page permutations pass.
- P0-2 remains incomplete: deep saved offsets before metadata, nonexistent/shrunk anchor cases, HOME pending-input drain before persistence, motion ownership, and unresolved-boundary extension still require implementation/verification. Do not equate current pixel tests with full UX qualification.
- HOME/close follow-up: reproduced retained 5,000px/s session velocity across HOME in a failing JVM regression, then froze velocity without changing position. Runtime now drains surface pending input before HOME persistence and before close marks the runtime closed (previously that flag rejected the drain).
- Passed full viewer/content/app unit tasks, debug APK + instrumentation APK builds and structure gate. New deterministic Android `ViewerPendingInputLifecycleTest` passes both HOME and close with 500px queued MotionEvent input before Choreographer delivery; asserts one input revision, exact persisted offset and stable foreground position. `.tmp-evidence-home-pending-input-20260905.txt`. This uses synthetic input against actual Android runtime plumbing, not the release corpus; no live episode credit. Physical HOME frame quality, complete motion ownership and all broader P0-2 cases remain unverified.
- P0-3 demand/retry: reproduced full control-buffer wake loss and unchanged demand reviving exhausted fetches (3 attempts became8). Replaced DemandMailbox + DemandAvailable signal with a separate conflated demand channel selected by the same pipeline actor; deleted the obsolete wrapper. Completion/control stays bounded and separate. No revision filter added based on the unsuitable existing sceneRevision.
- Previous demand is now retained until promotion comparison. Added deterministic saturation with100 alternating targets (only final target fetched), unchanged-demand exhaustion, and genuine-priority-promotion regressions. Existing eight pipeline scenarios plus these three pass (11), policy tests3 pass; content test fixture now uses the injected virtual clock, not wall time. App/content tests, debug/test APK builds and structure gate passed; after extra test strengthening, content tests and structure gate passed again.
- P0-3 is NOT complete: physical cancellation termination, bounded permits during cancellation, undelivered result cleanup, GL/CPU ownership and complete joined shutdown remain pending. Latest runtime APK from these pipeline changes has built but has not yet received live UX qualification.
- P0-3 handoff follow-up: a real pipeline regression reproduced leaked GPU texture when upload completed but its withContext return was cancelled. Decoder-return cancellation without a full send buffer did not fail in that first scenario; do not claim it did. Added atomic ResourceHandoff for CPU/GPU result transit, onUndeliveredElement disposal, shared send-failure cleanup, captured ownership inside upload context before return, and actor-finally buffered cleanup.
- Pipeline suite13 and ResourceHandoff suite4 pass. Additional checks cover buffered-channel cancellation, cancelled blocked sender (both cleanup paths still one release), receiver ownership, and8 competing cleanup threads. App tests, debug/test APK builds and structure gate passed after implementation; content tests/structure re-passed after additional regression tests. No live UX credit.
- Actual native cancellation termination/permits, renderer-queued CPU pin lifetime, and fully joined worker shutdown remain outstanding. These changes protect result delivery but do not establish that cancelling a coroutine immediately terminates native work.
- Renderer CPU lifetime follow-up: inspected actual C++ `viewerDescribeCpuTile` / nativeRelease; handles are raw pointers, not shared ownership. Moved CPU lease consumption to the renderer owner queue via uploadOnOwnerQueue; cancellation cannot free pixels during native upload. Upload port retains cleanup responsibility only before queue handoff and returns acquired texture on validation failure. Five deterministic queue/cancellation/success tests, app tests, debug APK builds and structure gate passed.
- Native emulator cadence regression FAILED twice after this change:42/369 then39/373 missed intervals. No >=100ms interval reported in these samples, but this is not a pass. First render p95=10.6794ms, second10.9836ms; second upload16.3252ms, decode17.6345ms, vsync missed1/434 while presentation missed39/373. Artifacts `.tmp-evidence-owner-queue-upload-20260905.txt`, `.tmp-evidence-owner-queue-upload-repeat-20260905.txt`, matching repeat-logcat and `.tmp-evidence-owner-queue-host-load-20260905.txt`.
- During repeat host CPU sampled81%,40%,48%,24%,30%; qemu dominated with574%,284%,228%,202%,185% per-process aggregate CPU (not normalized core percentage). After test host6..9%. AVD remains MangaViewerApi35 with -gpu host; SurfaceFlinger reports NVIDIA GTX1060 OpenGL translator, peak60Hz. Not a software-renderer fallback. This does NOT isolate app versus emulator causality: timing provenance remains flawed and trace work is required. Preserve both failures; no release credit or threshold relaxation.

## Presentation provenance and controlled renderer follow-up

- Native instrumentation confirmed this AVD selects EGL_COMPOSITION_LATCH_TIME_ANDROID (0x3436), not physical DISPLAY_PRESENT (0x343A). Added explicit timestamp provenance through JNI, OwnedPresentation, packed evidence, artifacts and strict verifiers. Latch/render-complete/swap-return records remain diagnostic and cannot satisfy physical-display or first-image latency gates. Synchronous native callbacks now wait for the actual submit duration before publishing; no artificial zero render duration.
- Verified the activity's first-presentation return value changes only its accessibility diagnostic marker, not UI visibility, input or image readiness. Extended the same provenance requirement into ViewerStartupTracker; regression proves all fallback kinds leave actual-first-image timing unset until an actual-display result arrives. App unit tests, debug/instrumentation builds and structure gate pass.
- `.tmp-evidence-display-provenance-gate-20260905.txt` correctly fails for missing actual-display timestamp. This is missing measurement support, NOT proof of good cadence or an exemption. Release count remains0.
- Captured 9,118,620-byte Perfetto trace `.tmp-evidence-render-provenance-20260905.pftrace` with SurfaceFlinger FrameTimeline, scheduling and gfx/view events. Renderer thread20625 in process20577: dequeueBuffer max77.708ms, eglSwapBuffers max60.122ms, emulator rcCreateSyncKHR encode max59.509ms, glBufferDataSyncAEMU encode max22ms. Display timeline contains deadline misses but does not correlate each viewer GL buffer with its input frame. These are observed blocking sections, not conclusive app-versus-environment attribution. Layer-specific SurfaceFlinger latency collection attempt had a shell syntax error and produced no evidence.
- Experimented with forwarding Choreographer expected presentation time into supported EGL_ANDROID_presentation_time (public EGL scheduling hint; never display evidence). Repeat diagnostics worsened to194 then139 latch intervals, second sample112.8094ms stall; no pass. Removed the entire experimental hint change with a scoped patch. Reverted run returned367 intervals, max82.2545ms, renderp95=10.855ms, but latch intervalp95=32.2921ms and missing physical timestamp still FAIL. Cannot attribute all variance solely to the hint; no speedup claimed.
- Evidence: `.tmp-evidence-presentation-hint-20260905.txt`, `...-repeat-20260905.txt`, `...-reverted-20260905.txt`, and `.tmp-evidence-presentation-hint-comparison-20260905.txt`. Host adapter rechecked: Ethernet Realtek2.5GbE at1Gbps only. No physical-device tests.
- Added isolated native pixel test using lossless6,001-row fixtures, three exact source-row tiles, scene replacement and forward/reverse offsets. Screenshots are independent compositor output, not metadata coverage claims. It is NOT a real-gesture or live-episode qualification; result pending at this checkpoint.

### Idle completion regression fixed and screenshot checks verified

- Initial pixel test failed on sourceRow2302 at screen center. Pulled and visually inspected `.tmp-evidence-native-pixels-first-20260905.png`: Android's white navigation handle overlays the debug probe's edge-to-edge Surface. This is not a decoded-row corruption. Gave the DEBUG probe a system-bar/cutout-inset root, rather than masking mismatched screenshot pixels. This changes the probe viewport, so earlier cadence measurements are not identical-viewport controlled comparisons. Production UI was not changed.
- Next still-frame run reproduced missing native completion within5s (`.tmp-evidence-native-pixels-insets-20260905.txt`). Current renderer queried once after20ms and never queried again when idle; pending results could retain scene references indefinitely. Replaced delayed Handler polling with one owner-thread Choreographer callback only while results remain pending. Native timestamp INVALID/query failure/one-second pending expiry now returns terminal UNAVAILABLE with time0; this is failure evidence, not a synthetic presentation. The recorder retains terminal records and never treats them as first-display success. No repeated draw, input lock or artificial gesture added.
- App unit tests (including terminal evidence and first-image provenance), debug/instrumentation APK builds, structure gate pass. The structure masker misreads C++ apostrophe numeric separators as char literals; wrote the same nanosecond constant in plain decimal without weakening the gate. Broader masker support remains a tooling limitation.
- Pixel test passes twice after completion fix:8 captures per run, every visible row at3 horizontal columns, source6,001rows across3exacttiles, two distinct color images, offsets0/1800/bottom/0 and scene replacement. `.tmp-evidence-native-pixels-completion-20260905.txt`, `.tmp-evidence-render-lifecycle-regressions-20260905.txt`. The latter reports OK(10tests): pixel1, pending-input HOME/close2, presentation verifier7. These are isolated regressions, not200live episodes.
- Subsequent cadence test still FAILS: timestamp kinds UNAVAILABLE and COMPOSITION_LATCH, no actual display support. `.tmp-evidence-frame-completion-cadence-20260905.txt`. Its current generic interval summary includes time0 terminal records; do not interpret that summary's enormous interval as an actual freeze. Keep all terminal failures in qualification; compute diagnostic positive-timestamp intervals separately if improving the summary. Performance/release unqualified; full corpus count remains0, no commit/push.

### Coverage and final-episode endpoint follow-up

- Previous goal turn classified progress: fixed real idle completion omission and verified independent screenshot regression, not merely status reporting.
- Reproduced two failing mapper tests: overlapping coverage of0..60 twice incorrectly claimed a full0..100viewport; randomized tile arrangements also produced false coverage. Replaced summed coverage with ordered interval union. Both tests now pass, including500 deterministic shuffled/overlapping/clipped arrangements compared against an independent per-unit occupancy oracle. This corrects evidence, not render behavior or pixel quality.
- Added page height and visible intra-page offset to telemetry from actual session layout. Auto-append harness no longer stops solely on entry into the requested last episode: it also requires the final page bottom to be visible with complete actual coverage. Unknown geometry, loading, overlap and partial last-page visibility cannot satisfy the endpoint. Corpus now passes its independently discovered episode IDs and compares manifest prefix and observed episode order. All original count/gesture/timeout/performance criteria remain.
- This endpoint is NECESSARY, NOT sufficient evidence that all earlier pages or all their pixels were traversed/presented. Full per-page coverage, tiny final episodes whose anchor cannot enter, warmup classification, owned-process memory and external-pixel verification for the live corpus remain unresolved. SessionTelemetryPlanner still reports overlap fields as0 based on its session invariant; independently deriving overlap remains an audit gap. Do not call the strict harness fully repaired or count live episodes from these unit tests.
- Passed `:viewer:test :viewer-content:test :app:testDebugUnitTest :app:assembleDebug :app:assembleDebugAndroidTest verifyArchitectureQuality`. Installed both APKs on emulator-5554. `.tmp-evidence-traversal-coverage-regressions-20260905.txt` reports OK(12tests): endpoint2, native screenshot1, HOME/close2, presentation verifier7. No live corpus run or release qualification in this batch. Full200count0, goal remains active, no main commit/push.

### Fetch preemption and physical cancellation follow-up

- Previous turn classified progress: fixed false viewport coverage and strengthened endpoint/independent-chain checks with executable regressions.
- Inspected current fetch preemption: it dereferenced demand with requireNotNull even after a viewport update removed that demand, and immediately assigned Absent after job.cancel(), releasing logical capacity before physical worker completion. Added a6page test filling4network slots with non-cancellable suspended fetch bodies, then retargeting to the final page. Before the fix the test failed with actor/channel cancellation; source inspection identifies the null-demand preemption path. Do not claim the old run reached the later overlap assertion.
- Preemption now handles absent demand as obsolete work, marks cancelRequested while retaining Fetching ownership, and releases the slot only on a generation/token-matched FetchStopped event from Job completion (asserts job.isCompleted). Stale success after requested cancellation is not published. One pending preemption is allowed at a time to avoid repeatedly cancelling all lanes while a worker is still stopping.
- Regression proves no fifth fetch starts while four physical fetch bodies remain suspended; after physical completion the new visible target starts exactly once. Full pipeline suite14, content tests, viewer tests, app unit tests, debug/test APK builds and structure gate pass. Installed APKs; HOME/close and native pixel checks pass again: `.tmp-evidence-fetch-cancellation-lifecycle-20260905.txt` OK(3tests).
- This is NOT full P0-3 completion. Decoder/upload retarget still assigns Idle immediately; generation replacement clears old records before all old workers terminate; joined shutdown and upload physical-completion contracts still need correction. Completion notification racing channel close should be explicitly stress-tested. No live episode or performance credit, corpus count0, no commit/push.

### Decoder cancellation follow-up

- Previous turn classified progress: changed fetch cancellation ownership with a passing physical-delay regression.
- Added HOME-return regression with a decoder body suspended in NonCancellable. Before modification it failed with two concurrent starts where only one was allowed. Decoding now retains its original token/lane and cancelRequested state across demand retarget, HOME and renderer-epoch cancellation until a matched DecodeStopped notification from completed Job releases it. Late cancelled results are closed instead of uploaded. Strengthened regression to5HOME/background cycles before releasing the old body; only1decode starts before physical completion and exactly1replacement starts afterward.
- Shared cancellation notification registration now handles a closed control channel without an unhandled child failure. It carries no resources. Refactored command receipt versus command dispatch into separate functions after the unchanged complexity15 gate rejected the enlarged combined function; no threshold exemption added.
- Pipeline suite15 and all content/app unit tests passed; debug/instrumentation APK builds and structure gate passed. Re-ran content/build/structure after strengthening the repeated-HOME test. Installed both APKs and passed Android HOME/close+native pixel regression3: `.tmp-evidence-decode-cancellation-lifecycle-20260905.txt`.
- Scope limit: decoded worker cancellation within the current generation is protected. Upload cancellation still returns logical Idle before the queued GL operation necessarily finishes, and generation replacement still discards old records; complete cross-generation physical capacity/joined shutdown remains pending. No performance improvement is claimed from narrow correctness tests. Full200count0, goal active, no commit/push.

### Upload physical completion follow-up

- Previous turn classified progress: same-generation cancelled decode ownership was fixed and verified.
- Strengthened queued-cancellation regression to assert Job is NOT completed before its renderer task runs. Old helper failed this assertion even though CPU pixel ownership itself was retained correctly. Upload helper now waits for owner cleanup in NonCancellable worker cleanup, including cancellation paths. It never waits on UI input/entry. A successfully resumed GPU key remains locally owned until cleanup and a cancellation check finish; abandoned keys are released exactly once.
- Uploading now retains cancelRequested/token/lane until a phase-matched completed-Job notification. DecodeStopped includes explicit upload phase so an older decode completion cannot release a newer upload with the same token. Stale cancelled uploads release their result. TextureUploadPort contract explicitly requires native work completion before cancellation finishes.
- Added pipeline regression holding an upload in non-cancellable cleanup over5HOME cycles: one upload until actual completion, one replacement afterward. Pipeline suite16 passes. OwnerQueueUploadTest suite6 passes, including cancellation after successful continuation resume while owner pixel cleanup is still executing (texture11 released once, no premature worker completion).
- Content/app tests, debug/instrumentation APK builds and unchanged structure gate passed; app unit/structure re-passed after extra cleanup-race test. Installed APKs and native pixel/HOME/close checks3 pass: `.tmp-evidence-upload-cancellation-lifecycle-20260905.txt`. Tests changed after install only add JVM coverage and port documentation; production behavior is the installed build.
- Cross-generation old-worker capacity and complete joined shutdown remain unfinished; do not mark P0-3 complete. Performance/cold latency/live200 verification still unqualified, count0, goal active, no commit/push.

### Joined shutdown follow-up

- Previous turn classified progress: verified upload worker lifetime now includes renderer cleanup.
- Reproduced closeAndJoin returning while a non-cancellable raw worker was still alive. Existing implementation joined only the actor then called ownerJob.cancel without joining child termination. Introduced an instance-owned PipelineShutdown latch: one caller initiates close, all concurrent callers await the same completion, and non-cancellable coroutine cleanup cancelAndJoins the owner and its children. Scheduling stops once shutdown starts. No UI thread blocking or input gating was added.
- Regression now covers3additional concurrent closers and cancellation of the first closer while native-like work remains suspended. None returns early; all finish only after the worker terminates; repeated close after completion succeeds. Pipeline suite17 passes. Content/app tests, debug/test APK builds and unchanged structure gate pass.
- Runtime already orders pipeline close before renderer destruction. Wrapped renderer close in finally so a pipeline-close exception cannot skip renderer/runtime disposal after the joined cleanup. Rebuilt and rechecked app unit/structure, installed APKs and passed HOME/close+native pixel checks3: `.tmp-evidence-joined-shutdown-lifecycle-20260905.txt`.
- This verifies owned child-job shutdown, not every provider's external process/socket lifetime. Generation replacement while the pipeline remains open still clears old records before their physical workers finish, so cross-generation capacity remains pending. P0-3 overall and200live/performance qualification still incomplete; count0, goal active, no commit/push.

## Cohesive lifetime integration and live NTK checkpoint

- User objected to piecemeal fixes and tiny-test progress reports. Acknowledged the need to address cancellation, generation transitions and shutdown together and to report remaining root defects/live completion rather than imply small test counts equal UX completion.
- Interrupted generation test was re-polled using its actual session handle; it had terminated with2failures: generation replacement started2same-page fetches/decodes instead of1. No in-flight operation was assumed complete without inspection. Last status-only reply did not change code; this batch implements the reproduced gap.
- Added actor-owned retiring work records retained across generations until physical Job completion. Pending old fetches consume network capacity; pending decodes/uploads consume their lane and block same-page replacement across lanes. Retired records have no demand and never publish stale results. Completion events drive cleanup; no polling/timer or input/entry lock. Retired resident lists are released/cleared once. Shutdown still joins the owning Job tree and drops tracking after actor termination.
- Snapshots now explicitly include retiring page records and aggregate old+new active counts. App telemetry lists those active IDs but keeps current-generation manifest progress separate. This prevents old work being hidden by a generation reset.
- Pipeline suite19 passes. Added PipelineLifecycleIntegrationTest: for fetch/decode/upload separately, hold physical work through8generations mixed with HOME/background and renderer epoch changes, verify only1phase owner and1retiring record, then close before releasing the old worker; close waits, no replacement starts during shutdown, and no texture remains. All3phase scenarios pass. Existing tests separately verify work resumes after old completion.
- Passed viewer/content/app unit tests, debug+instrumentation builds and unchanged structure gate. Installed APKs. Checked host network: only Ethernet1Gbps up; emulator-5554 used, no physical device.
- Ran live ViewerNtkSmokeTest with its default NTK episode, no test warm helper/cache clearing. Actual initial swipe succeeded. Pulled/viewed `.tmp-evidence-lifetime-ntk-live-20260905.png`: comic image is visibly rendered inside the system bars. This screenshot is NOT a4second latency proof or full correct-image verification.
- Live test FAILS after28.105s waiting for its physical-first-presentation marker. Native log selects COMPOSITION_LATCH0x3436; the marker intentionally does not count it as actual display evidence. Thus the test's generic 'image not presented' message does not mean the screenshot was blank. Keep failure, no corpus credit; replace measurement with independently justified display evidence rather than accepting the fallback.
- `.tmp-evidence-lifetime-ntk-live-logcat-20260905.txt`: startup WebView410ms; document ready1045ms; parse754ms; ACK-ready2187ms from its own phase origin. These are stage-specific diagnostic values, not comparable full first-image latency. Page candidates p5..p8 report Cronet ERR_CONNECTION_RESET; external transport vs app cancellation is NOT isolated yet. GC also has a27.680ms pause in this diagnostic. Do not attribute all remaining delay to ACK or emulator, and do not claim stutter-free.
- Known fetch/decode/upload cancellation, generation accounting, and joined-shutdown defects now have coherent code plus mixed lifecycle coverage. Full real-session stress, provider physical cancellation contracts, P0-2/P0-4/P0-5/P0-6/P0-7 and strict corpus completeness/provenance audits remain. Performance/cold startup/200live count still0; no goal completion, commit or push.

## NTK route timeout and cancellation classification

- Previous turn classified progress: coherent lifetime accounting, mixed lifecycle test and live evidence established next transport/provenance work.
- Found NtkReplicaRacer used withTimeout then caught all CancellationException as abandoned and rethrew. Its own timeout therefore cancelled only the candidate child without sending Outcome.Failed; the race collector still awaited the original job count and could wait indefinitely. Reproduced: configured25ms attempt never finished until enclosing1000ms test deadline fired. Changed only locally owned timeout to withTimeoutOrNull + SocketTimeoutException; external cancellation still propagates. Regression now ends with a normal route failure at25ms. Additional validation-prefix stall regression proves response closed once and race terminates at25ms.
- Found complementary body-read error: ReplicaTrackedPageByteStream caught an ENCLOSING TimeoutCancellationException and converted it to a2000ms upstream no-progress error. Reproduced an outer1ms deadline being charged as route failure. Body-owned progress timeout now uses withTimeoutOrNull; its timeout closes/fails the route, while external deadline remains cancellation/abandonment without route-health penalty. Existing stall and explicit cancellation tests plus new enclosing-deadline regression pass.
- Unchanged60line gate caught executeAttempt at62lines; extracted immutable request construction/body-budget documentation into a small function, preserving timeout values and avoiding exemptions.
- Passed NTK/data/content/app unit tests, debug+instrumentation builds and structure gate. Added HttpEngineExchange terminal provenance logging ONLY after takeCompletion wins: engine_failure_callback vs engine_cancel_callback vs local_abort. Thus a late callback after local cancellation cannot be misreported as the terminal cause. Rebuilt/retested data and APKs after instrumentation.
- Live diagnostic on `/webtoon/57451201/jjaptoon-1313625`, untouched cache and no test warm helper: `.tmp-evidence-ntk-timeout-terminal-20260905.txt` FAIL28.043s waiting for actual-display marker (AVD still composition-latch only). `.tmp-evidence-ntk-timeout-terminal-logcat-20260905.txt` process24152 has accepted engine_failure_callback NetworkExceptionWrapper ERR_CONNECTION_RESET before headers, followed by matching candidate failures. Normal loser/app cancellations separately show local_abort. This narrows attribution to transport-engine reported failure, not an already-completed app cancellation; it does NOT prove which network hop/server/emulator layer caused the reset.
- Diagnostic stage timings: document1088ms, parse735ms, current ACK2355ms and later next ACK2036ms (different clocks/origins, not first-image end-to-end). Late screenshot `.tmp-evidence-ntk-timeout-terminal-20260905.png` was inspected and is Android HOME after the scenario closed; it is NOT image-rendering evidence.
- Inspection for next action: multi-origin non-FOCUS races initially use preferred protocol for every origin, but alternate protocol hedge only runs for FOCUS or single-origin windows. Protocol registry remembers only full-body completions. Early forward pages may therefore exhaust multiple QUIC-preferred pools while only the focus has an HTTP2-preferred hedge. This is a hypothesis to test against structured transport outcomes, not grounds to force a protocol or relax the gate. `preferQuic` is engine preference, not proof of negotiated HTTP3.
- Remaining live transport resets and physical presentation measurement are unresolved. Timeout deadlock/classification fixes do not establish a latency/stutter win. No live corpus qualification,200count0, goal active, no commit/push.

## Sequential protocol experiment rejected after live check

- Previous turn classified progress: fixed timeout deadlocks and distinguished accepted engine failures from local aborts. This batch tested the protocol-selection hypothesis rather than assuming it explained the resets.
- Implemented an experimental sequential alternate-protocol request only after pre-header IOException for multi-origin non-FOCUS attempts (which had no protocol hedge). It reused the same attempt admission/deadline and changed only preferQuic, not URL/headers/body/quality. Unit reproduction previously failed all origins; experimental route recovered with peak1. Additional4tests verified HTTP403 was not retried, a shared25ms outer deadline was retained, cancellation never retried, and unsupported protocol pools were not duplicated. NTK/app tests, builds and structure passed. These tests establish the experiment's behavior, NOT real provider benefit.
- Live episode `/webtoon/57451201/jjaptoon-1341148` with untouched cache: `.tmp-evidence-ntk-sequential-route-20260905.txt` FAIL27.426s for actual-display marker. `.tmp-evidence-ntk-sequential-route-20260905.png` captured during the run was viewed and shows comic pixels; not a4s latency/identity/stutter proof. Process24472 log `.tmp-evidence-ntk-sequential-route-logcat-20260905.txt` continues to show engine-failure resets and terminal candidate failure with sequentialFallback=true, including p0082/p0085. Both attempts can fail; protocol switching alone did not restore these routes. Saved experiment XML results as `.tmp-evidence-sequential-protocol-tests-20260905.xml` and `...-race-tests-20260905.xml`.
- REVERTED the entire experimental production change and its feature-specific tests/helpers to avoid shipping unproven extra requests/latency. Original timeout regressions, cancellation guards and terminal diagnostics remain. No existing qualification test removed or threshold changed. Experimental behavior/results remain in artifacts/documentation. Rebuilt NTK tests/debug+test APKs/structure successfully and reinstalled the reverted APKs on emulator-5554; device is NOT left on the experimental version.
- Important current-source finding: NtkReplicaRacer's DEFAULT maxConcurrentAttempts is6, but ACTUAL NtkPageService config explicitly passes maxConcurrentAttempts=24 and visibleReservedAttempts=6 (lines48/49 at inspection). Do not infer live transport bound from the default or the single-slot fake test. Actual request/ACK/manifest concurrency and document-cookie-episode consistency need joint inspection. Repeated resets could involve provider rejection or transport behavior; no proof yet that the emulator, QUIC preference or external network alone caused them.
- Next focus is current ACK/document/image access consistency and actual shared request budget, using the completed Astra review's source-boundary findings. No new Oracle request made for this bounded protocol experiment. Performance/live200 remains unqualified, count0, goal active, no commit/push.

## Access preparation identity and physical image ownership

- User challenged the repeated narrow-fix cycle. Response explicitly separates local regressions from complete UX qualification: full200 remains0. This batch follows document identity -> cookie readiness -> ACK/image request replacement -> resource release, rather than changing performance thresholds or adding protocol retries.
- NtkDocumentClient previously observed finalUrl before checking response status, before reading the full document, and before verifying the requested episode path. NtkDocumentOriginTest reproduced failures for rejected HTTP, wrong-episode redirect, and incomplete bodies (3of4 tests failed before modification). Only a selected, fully read, successful document with the expected episode URL now updates the origin. Generic text fetch also observes only after successful full read. This does NOT yet make origin publication conditional on parsed manifest semantics, nor prevent an older successful concurrent navigation from publishing last.
- Prepared page requests now retain episodeOrigin from their exact document. Image Referer and gateway authorization/page-access keys use that bound origin, not a later mutable navigation origin. A regression changes global origin via another navigation after preparing a page; it previously changed the old page's Referer and gateway key, and now preserves both. Manifest cleanup covers original and redirected document keys. Four origin tests and the page binding regression pass.
- CookieManager.setCookie's Boolean was ignored. Cookie batches now return acceptance only after all writes succeed; refusal stops the batch. Document cookies must be accepted before document-ready or authorization/manifest delivery. Identity-cookie refusal terminates the request; warm preparation failure clears pending preparation and fails an active matching request instead of leaving it waiting. Retired document callbacks cannot fail or resume another request. Four pure callback tests pass. NtkBrowserDocumentControllerTest7 passed on emulator-5554, including rejected-cookie and stale-failure cases. These are injected cookie outcomes, not proof that every live provider cookie is accepted.
- ACK-first request replacement previously called direct.cancel without awaiting physical termination. Added a non-cancellable transport-cleanup regression: old code started2requests with peak2 before cleanup. New authorization/page race uses an owned rendezvous result channel, joins cancellation before replacement, and releases undelivered or selected-but-not-returned pages. Old request must finish before a replacement begins. Three dedicated tests include100 simultaneous ACK/page completion permutations and caller cancellation while authorization cleanup is pending. One test initially used exception object identity; coroutine stack-trace recovery copies exceptions, so it now verifies exception type/message and unchanged attempt count.
- The deeper replica race also leaked a validated selected body when the caller cancelled while joining a losing request. NtkReplicaCancellationTest reproduced closes=0 instead of1. A bounded NtkReplicaRaceOwnership now retains every validated candidate until child jobs finish, transfers only the chosen body, and closes/releases the rest on all exits. Candidate producers stop owning bodies/leases immediately upon transfer to the race. Response and validated-page references no longer both own the same successful body. Image prefix validation leaves error cleanup to the replica owner rather than closing the same response twice. The old inline result collector was split into a selection function; no extra scheduler, timers or retry policy were added. New cancellation regression passes. Additional ownership tests cover exactly-once transfer and continuing cleanup after one body close throws.
- NTK/content/app unit tests, debug and instrumentation APK builds, and unchanged architecture gate passed before the additional close-error coverage; final verification follows below. Installed this batch's APKs and passed native pixel/HOME/close3: `.tmp-evidence-access-ownership-native-20260905.txt`.
- Live diagnostic with unchanged cache and default episode1341148: `.tmp-evidence-access-ownership-live-20260905.txt` FAIL27.439s on actual-display marker. During-run screenshot `.tmp-evidence-access-ownership-live-20260905.png` was inspected and shows comic pixels inside the bars. Logs still select COMPOSITION_LATCH0x3436, not DISPLAY_PRESENT; failure is retained. No4s proof or corpus credit. Latest capture contains no matching new image transport failures for process25138, but existing cached pages make this inconclusive; do NOT claim connection resets fixed.
- `.tmp-evidence-access-ownership-live-logcat-20260905.txt` and `...-live-final-logcat...`: document695ms; parse945ms (normalize311, DOM170, images12, JSON451); browser ACK-ready2315ms from its phase origin. These clocks are not end-to-end first-image latency. Normalization and whole-document JSON traversal remain substantial measured application work; the dedicated, escape-correct manifest reader from Astra P0-6 is still outstanding. Physical presentation measurement, strict corpus completeness, next-episode ownership, and the broader remaining P0 contracts remain unresolved. No goal completion, commit or push.
- Final verification after close-error coverage: NTK127 unit tests0failures; app unit tests, both app APK builds, source instrumentation build and unchanged structure gate pass. Content tests also passed in the preceding batch. Reinstalled final app/test/library-test APKs; cookie/document Android7 pass again, saved `.tmp-evidence-access-cookie-barrier-20260905.txt`. `git diff --check` exits0 (line-ending notices only). No in-flight command remains. The full release/lint/corpus gate has NOT been run or passed. Goal stays active,200count0.
