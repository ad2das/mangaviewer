<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- The app's own default is the light palette; the runtime rewrites system bars per setting. -->
    <style name="Theme.MangaViewer" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:fontFamily">sans</item>
        <item name="android:colorAccent">#6045FF</item>
        <item name="android:windowBackground">#F5F7FA</item>
        <item name="android:navigationBarColor">#F5F7FA</item>
        <item name="android:statusBarColor">#F5F7FA</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowActionModeOverlay">true</item>
    </style>
</resources>
